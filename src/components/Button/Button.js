import React from 'react';

const Button = (text, on_click, ) => {
    return (
        <div className={"input__btn"}>
            <a type="button" onClick={handleNormalize}>
                Проверить состав</a>
        </div>
    );
};

export default Button;