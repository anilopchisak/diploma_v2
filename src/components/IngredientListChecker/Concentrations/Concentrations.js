import React from 'react';
import './Concentrations.css'

const Concentrations = (normText) => {
    let _normText = normText;

    return (
        <div>
            conc
        </div>
    );
};

export default Concentrations;